// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBegA2osr7aBLFVe0kmMxkHtOMtSTUIY8s",
    authDomain: "proyecto-dps.firebaseapp.com",
    databaseURL: "https://proyecto-dps.firebaseio.com",
    projectId: "proyecto-dps",
    storageBucket: "proyecto-dps.appspot.com",
    messagingSenderId: "736248700657",
    appId: "1:736248700657:web:201c5dda1a9da43e2c5350",
    measurementId: "G-M42VE7TXE0"
    }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
